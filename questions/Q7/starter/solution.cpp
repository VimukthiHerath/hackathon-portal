#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> arr(n);
    for (int i = 0; i < n; i++) cin >> arr[i];
    int target;
    cin >> target;

    // TODO: binary search for target in arr and print its index, or -1
    cout << -1 << endl;
    return 0;
}
