import sys

def solve():
    s = sys.stdin.readline().rstrip("\n")
    # TODO: find the first character in s that appears exactly once,
    # and print it. Print -1 if every character repeats.
    print(-1)

if __name__ == "__main__":
    solve()
